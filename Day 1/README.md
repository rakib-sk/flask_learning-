# Day 1  
## First Flask Code  

### 📌 সংক্ষিপ্ত ব্যাখ্যা  
এই কোডটি Flask-এর সবচেয়ে বেসিক (Hello World) প্রজেক্ট।  
এখানে আমরা Flask ইমপোর্ট করেছি, একটি অ্যাপ অবজেক্ট বানিয়েছি,  
একটি route (`/`, `/about`, `/contact`) সেট করেছি, এবং শেষে সার্ভার রান করিয়েছি।  
এটি Flask শেখার প্রথম ধাপ এবং প্রতিটি ওয়েব অ্যাপ এই বেসিক স্ট্রাকচার দিয়ে শুরু হয়।  

---

## 💻 Code  

```python
from flask import Flask  # Import Flask 

app = Flask(__name__)  # Create Flask object

@app.route("/")  # @app is a decorator
def hello():
    return "Hello This is my first Flask app"

@app.route("/about")
def about():
    return "This is about page"

@app.route("/contact")
def contact():
    return "This is contact page"

# Run Server
if __name__ == "__main__":
    app.run(host="0.0.0.0", port=5000)
```

---

## 🧠 কোডের ব্যাখ্যা

### 1️⃣ `from flask import Flask`
Flask লাইব্রেরি ইমপোর্ট করা হচ্ছে যাতে আমরা Flask অ্যাপ তৈরি করতে পারি।

### 2️⃣ `app = Flask(__name__)`
এটি একটি Flask অ্যাপ অবজেক্ট।  
এই app অবজেক্টই পুরো ওয়েব অ্যাপটি হ্যান্ডেল করবে।

### 3️⃣ `@app.route("/")`
এটি একটি **route decorator**।  
মানে: কেউ ব্রাউজারে `/` এ গেলে নিচের function রান করবে।  
যেমন → `http://127.0.0.1:5000/`

### 4️⃣ তিনটি route বানানো হয়েছে  
- `/` → Home  
- `/about` → About page  
- `/contact` → Contact page  

প্রতিটি route ব্রাউজারে ভিজিট করলে আলাদা text return করবে।

### 5️⃣ সার্ভার রান করা  
```python
app.run(host="0.0.0.0", port=5000)
```
- Host `0.0.0.0` → সব জায়গা থেকে অ্যাক্সেস করা যাবে  
- Port `5000` → Flask এর default port

---

# 📝 HTTP Methods (GET & POST)

### GET  
- শুধু data **দেখা** বা **নেওয়ার** জন্য  
- URL এ query পাঠানো হয়  
- উদাহরণ: `/search?name=rakib`

### POST  
- data **send** করার জন্য  
- Form submission, login, register ইত্যাদি কাজের জন্য

---

# 🔥 Core Concepts of Flask  

### 1️⃣ **request**
- ক্লায়েন্টের পাঠানো data পড়তে ব্যবহৃত  
- যেমন: form data, query data, JSON data

### 2️⃣ **redirect**
- একজন user কে অন্য route বা পেজে পাঠাতে ব্যবহার করা হয়  
- উদাহরণ: লগইন না থাকলে login পেজে redirect করা

### 3️⃣ **Response**
- সার্ভার user কে যা পাঠায় সেটিই response  
- এটি text, HTML, JSON—যেকোনো কিছু হতে পারে

### 4️⃣ **Session**
- ব্যবহারকারীর temporary data সংরক্ষণ করতে ব্যবহৃত  
- যেমন: user login data, cart data

### 5️⃣ **url_for**
- directly route লিখার বদলে function name দিয়ে URL তৈরি করা  
- কোড clean ও bug-free রাখে  
- Example:
```python
url_for('home')  # Gives URL of home route
```

---

# ✔ Day 1 Summary  
- Flask install  
- Basic app run  
- 3 routes `/`, `/about`, `/contact`  
- GET & POST কি  
- request, redirect, session সহ core concept জানা  

---

# 🎯 পরবর্তী ধাপ  
Day 2 তে থাকবে:  
- HTML template  
- Form submit  
- POST request handle  
- Dynamic route  
- JSON response