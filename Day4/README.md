# Flask Modular App — Project Structure & Explanation

এই প্রজেক্টটি একটি **Modular Flask Application**, যেখানে কোডকে বিভিন্ন ফোল্ডারে ভাগ করা হয়েছে যাতে প্রজেক্ট বড় হলেও মেইনটেইন করা সহজ থাকে।  
নিচে পুরো ডিরেক্টরি স্ট্রাকচার, কাজের প্রক্রিয়া এবং রান করার নিয়ম দেওয়া হলো।

---

## 📂 Project Structure

```
project/
│
├── app/
│   ├── __init__.py
│   │
│   ├── models/
│   │   └── __init__.py
│   │
│   ├── routes/
│   │   ├── __init__.py
│   │   ├── profile.py
│   │   └── dashbord.py
│   │
│   ├── static/
│   │   ├── CSS/
│   │   │   └── style.css
│   │   ├── JS/
│   │   │   └── script.js
│   │   └── images/
│   │       └── demo.jpg
│   │
│   └── templates/
│       ├── base.html
│       └── home.html
│
└── run.py
```

---

## 🧠 Folder Explanation

### **1. app/__init__.py**
এখানে Flask app create করা হয় এবং  
Blueprint, Database, Configuration সব initialize করা হয়।

### **2. models/**
এখানে ভবিষ্যতে Database Models থাকবে।  
উদাহরণ: User, Post, Profile Info ইত্যাদি।

### **3. routes/**
এখানে সব রাউট আলাদা ফাইলে রাখা হয়েছে যাতে কোড পরিষ্কার থাকে।

- **profile.py:**  
  ইউজারের প্রোফাইল রিলেটেড পেজ/লজিক এখানে থাকবে।

- **dashbord.py:**  
  Dashboard এর UI ও Backend রাউট এখানে থাকবে।

### **4. static/**
Front-end এর ডিজাইন ফাইলগুলো এখানে:

- **CSS/style.css:** ওয়েবসাইটের ডিজাইন  
- **JS/script.js:** ক্লায়েন্ট সাইড স্ক্রিপ্ট  
- **images/demo.jpg:** ডেমো ছবি/অ্যাসেট

### **5. templates/**
HTML ফাইলগুলো এখানে রাখা হয়েছে:

- **base.html:** মূল layout (Navbar/Footer)  
- **home.html:** হোম পেজ / Dashboard UI

### **6. run.py**
এই ফাইল রান করলে Flask সার্ভার স্টার্ট হবে।

---

## ▶️ How to Run

### **Step 1: Create Virtual Environment**
```
python -m venv venv
```

### **Step 2: Activate Virtual Environment**
Linux/Termux:
```
source venv/bin/activate
```

Windows:
```
venv\Scripts\activate
```

### **Step 3: Install Required Packages**
```
pip install flask
```

### **Step 4: Run the Project**
```
python run.py
```

Server চলবে:
```
http://127.0.0.1:5000
```

---

## 🚀 Features

- Clean Modular Structure  
- Blueprint-based Routing  
- Static & Templates Folder Ready  
- Future Ready for Database Integration  
- Easy to Scale

---

## 📌 Future Improvements

- User Login System  
- Database Models (SQLAlchemy)  
- Admin Dashboard  
- API Endpoints  
- Authentication Middleware

---

## ✔️ Conclusion  
এই স্ট্রাকচার ব্যবহার করলে Flask প্রজেক্ট বড় হলেও কোড ম্যানেজ করা খুব সহজ হয়।  
রাউট, মডেল, UI — প্রতিটি আলাদা ফোল্ডারে ভাগ করা, তাই Maintainability এবং Scalability দুটোই বাড়ে।
